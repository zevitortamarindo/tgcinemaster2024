class Streaming {
  String? title;
  bool? done;
  String? image;

  Streaming({this.title, this.done, this.image});
}

class Filme {
  String? title;
  bool? done;
  String? image;

  Filme({this.title, this.done, this.image});
}
